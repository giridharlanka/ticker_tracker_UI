# Copy to .env and fill in (never commit .env).
# Loaded automatically when you run app.py or ticker-tracker.

# Gemini Flash (optional; or use Settings → keychain)
# GEMINI_API_KEY=

# Alternative env name (same API key from Google AI Studio)
# GOOGLE_API_KEY=
