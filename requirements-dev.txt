# Development / CI dependencies (install after requirements.txt).
# pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt
pytest>=8.0.0,<9
pytest-cov>=4.1.0,<6
ruff>=0.8.0,<0.10
mypy>=1.11.0,<2
